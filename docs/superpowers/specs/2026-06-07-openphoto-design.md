# OpenPhoto — Design Spec

**Date:** 2026-06-07
**Status:** Approved by Jude (brainstorming session)
**Repo:** https://github.com/jude-sph/OpenPhoto.git
**Raw requirements:** [docs/SPECS.md](../../SPECS.md)

## 1. What this is

A native macOS photo manager built on one promise: **the library is just files**. Photos and videos live in the user's existing folders (`~/Pictures`, `~/Movies` — arbitrary, nestable folders like `rome2022` or `mac-screenshots`), exactly as they are. The app indexes, views, imports, syncs, and analyzes — but the library remains fully usable and browsable with the app deleted. The Obsidian philosophy applied to photos: forward compatibility, plain files, no lock-in.

**Hard invariant:** originals are never modified or moved without explicit user action. All app-generated data lives in sidecars or a rebuildable index.

## 2. Decisions log

| Decision | Choice |
|---|---|
| Stack | Native Swift + SwiftUI (Vision, Core ML, ImageCaptureCore, MapKit); SQLite via GRDB |
| Architecture | Vault + Catalog (option A): self-describing vaults everywhere, rebuildable live catalog on the Mac, catalog snapshot on drives |
| Mac ↔ drive topology | Mac = working subset + full catalog of everything; canonical drive = everything |
| Working set control | Per-folder pinning; explicit evict, never automatic |
| Delete vs evict | Strictly separate operations. Delete → vault bin, propagates to drive bin only after explicit review. Evict → releases verified-synced local copy, propagates nothing |
| Folder semantics | Folders are arbitrary and nestable; folder ≠ event; videos mixed with photos in folders |
| Sidecar format | Per-file XMP, full-name convention: `IMG_4123.heic.xmp` |
| Sidecar location | Hidden subfolder per folder: `rome2022/.openphoto/IMG_4123.heic.xmp` (folder stays self-contained; Finder stays clean; convertible to beside-file layout by a one-click export) |
| Asset identity | BLAKE3 content hash; `(path, size, mtime)` fast-path on rescan |
| Intelligence | 100% local (M4 Pro); derived at import time so offline photos stay searchable |
| Scale target | ~200–250 GB now (≲100k items); design incremental so 10× doesn't break |
| First milestone | Browse (index + timeline + folders + viewer) |

## 3. Core concepts

### Vault
A folder tree that is a library location. Mac library = two vaults (`~/Pictures`, `~/Movies`). A drive carries one vault whose top level mirrors the Mac vault roots by name (`Pictures/`, `Movies/`). Each vault root has a `.openphoto/` directory:

- `manifest.jsonl` — one line per asset: content hash, vault-relative path, size, mtime. Plain text: greppable, diffable, rebuildable from the filesystem.
- `sync-log.jsonl` — append-only journal of import/sync sessions.
- `bin/` — deleted items with original relative paths preserved.

A vault is self-describing: any Mac running OpenPhoto opens it as a library.

### Asset identity = content hash
The BLAKE3 hash of file bytes is the asset ID. Renames and moves (even in Finder) preserve identity — rescan matches the hash and updates the path; metadata, faces, and history follow. Originals are immutable by invariant, so hashes are permanent. Dedup is structural: same hash in two places = one asset, two instances. A Live Photo is one logical asset of two files (HEIC + MOV paired via Apple's content identifier metadata), each hashed individually.

If a file's bytes ever change in place (an outside app edited it), the new hash is treated as a **new asset** and the event is flagged for review; archived copies of the old asset are never silently overwritten.

### Catalog
Live SQLite database at `~/Library/Application Support/OpenPhoto/` plus a content-addressed thumbnail cache. Knows every asset across all vaults, online or offline. **Strictly rebuildable** from vaults (slowly — ML re-runs). Core tables:

```
assets:      hash PK, kind (photo/video/live), taken_at, gps, dims, camera, live_pair_hash, …
instances:   hash → (vault_id, rel_path, size, mtime)      ← the presence map
vaults:      id, role (local/canonical/backup), root path or volume ID, last_seen
folders:     materialized tree across vaults (incl. offline)
faces:       asset hash, bbox, embedding, cluster_id, confidence
persons:     named clusters; user merge/split state (human anchors)
ocr:         FTS5 full-text per asset
embeddings:  CLIP vector per asset
jobs:        per-asset per-stage pipeline completion (resumable)
queues:      pending sidecar writes for offline assets; pending delete propagations
```

The presence map answers at any moment: *what exists only on the Mac and isn't backed up anywhere?* The sync screen leads with that number. Drive vaults remain browsable while unplugged via their last-known manifest + catalog data (thumbnails, metadata, search all work; full-res asks for the drive).

Each sync refreshes a **catalog snapshot** (`<drive vault>/.openphoto/catalog-snapshot/`: catalog + thumbnails) onto the drive, making the drive a complete library *with intelligence included* — plug into a fresh Mac and browse/search immediately, no ML re-run. The Mac catalog is the only live copy; the snapshot is write-only at sync end (never merged back — except during migration adoption, §5.5).

### Metadata rule
Anything a **human authored** — tags, ratings, captions, people names (as MWG face regions) — is written to standard XMP sidecars in the per-folder `.openphoto/` subfolder. Anything a **machine derived** — thumbnails, embeddings, face vectors, OCR, clusters — lives only in the catalog. Sidecar edits to offline assets are queued in the catalog and applied to the drive's sidecar files at next sync. The app self-heals sidecar associations via content hash after outside renames/moves.

## 4. Deletion model

- **Delete** = move file + sidecar into the vault's `.openphoto/bin/` (original path preserved), record the event, queue for propagation. One-click restore. Emptying the bin moves files to macOS Trash — still recoverable. Nothing in the system ever hard-deletes.
- **Propagation**: at next drive sync, a review screen lists deletions since the last sync; on confirmation, drive copies move to the *drive's* bin. Review is available **standalone** (a "Review Deletions" gate on the drive, any time it's connected) as well as inside the sync plan; both share one confirm and move copies into the drive vault's `.openphoto/bin/` (`origin:"propagated"`).
- **Evict** = an unrelated operation: release a folder's local full-res copies (to macOS Trash) once every item is hash-verified on the canonical drive. Catalog keeps the folder browsable/searchable. Reversed by **rehydrate**.

## 5. Sync engine

**Design resolution: drives are passive; every flow is one-way; there is no merge logic in the system.** Human gates exist in exactly three places: import selection, delete propagation, drift review.

### 5.1 Import (phone via ImageCaptureCore / SD via mounted DCIM → Mac)
Big grid of large thumbnails; already-imported items badged (pre-download heuristic: name+size+date; hash-certain after copy). User selects items and a destination folder (existing or new). Pipeline per item: **copy → hash → dedup-check → place → manifest + catalog update → verify**. Only verified items are offered for device deletion. Live Photo pairs import as one asset. Import sessions are recorded in `sync-log.jsonl`.

### 5.2 Sync (Mac → canonical drive)
On plug-in: read drive manifest → reconcile → present a **plan** before touching anything: new files (with sizes), sidecar updates, folder renames, deletions to review. Properties:

- Folder renames/moves propagate as renames (hash matching) — no recopying.
- Every copy: temp file → fsync → re-hash verify → rename into place → then record in both manifests. Interrupted sync leaves only temp files; re-run is idempotent and resumes.
- Disk space pre-checked in the plan; failure happens before copying, not during.
- Catalog snapshot refreshed last, after all verification.

### 5.3 Evict / Rehydrate
Per-folder. Evict requires full hash-verification on canonical. Rehydrate copies a folder back from the drive, verified the same way.

### 5.4 Clone & backups (canonical → backup)
The Mac cannot populate backups alone (it lacks evicted originals), so backups are made by cloning canonical → backup with **both drives plugged in** — manifest-driven, hash-verified, identity-mapped (a mirror: the backup's layout equals the canonical's). Any number of backup drives; the catalog tracks per-drive presence.

**The canonical is the source of truth; backups are derived from it.** A backup is a full redundant copy whose contents are *defined by* the canonical. The principle:

- **Reads treat all connected durable drives (canonical + backup) as equal** — any of them satisfies "backed up", and any is a valid source for browse, rehydrate, send-from-drive, and verified-evict (re-hashing a backup copy is proof as good as canonical). When several are connected, **canonical is preferred** as the read source.
- **Writes flow one-way, canonical → backup, only.** A backup never feeds back into the canonical (no merge — except the explicit migration flag-flip, §5.5). Additions always flow *through* the canonical: a backup never receives a photo before the canonical has it. When a backup deviates from the canonical, the backup is **wrong** and is repaired *from* the canonical (drift restore), never the reverse.

**Keeping backups current — no byte caching, no bloat.** When the canonical gains photos (a new import synced Mac→canonical) or loses them (a deletion), other drives may be unplugged. The Mac never stages the changed *bytes* — the canonical **is** the byte store. It remembers only tiny metadata it already has: **per-drive presence** (which hashes each drive holds, in the catalog) and **pending deletions** (just hashes). Bringing a backup up to date is therefore two independent streams:

- **Additions** = a re-runnable, diff-driven clone (canonical manifest − backup manifest = the missing files), copied canonical → backup. This requires the canonical *and* the backup both connected (the bytes live on canonical). Nothing accumulates on the Mac, so adding GBs to the canonical while a backup is away costs the Mac nothing — the diff is recomputed and copied the next time both are present.
- **Deletions** = per-drive cached instructions (a hash each). A deletion stays pending **for every drive that still holds the photo** and applies (through the Review-Deletions gate) when that drive next connects — needing only that drive (the instruction is the cache). It clears for a drive once that drive no longer holds the hash, and clears entirely once no drive holds it. So a deleted photo can never resurrect from an unplugged backup, and the only thing "remembered" indefinitely is a hash.

On connect, the app already knows from the catalog whether a backup is behind, and offers to **update it** — copy the additions (if the canonical is also connected) and review+apply its pending deletions (always). If the canonical is absent, deletions still apply and the additions are reported as pending ("connect the canonical to copy N photos").

### 5.5 Canonical management & migration
Exactly **one** drive is the canonical (the source of truth); the **catalog role is authoritative**, and each drive self-describes its role in `vault.json` (reconciled to the catalog when it reconnects).

- **Change the canonical** — "Make this the canonical" on a connected backup, gated on **exact agreement** (the backup must hold *exactly* the canonical's hashes — nothing missing, nothing extra; a behind/drifted backup is *not* a candidate, you "Update backup" first). The cheap UI gate compares the catalog's per-drive presence sets; promotion **re-verifies against both `manifest.jsonl`s** before acting. The flip is **atomic in the catalog** (`setCanonical`: promote-new + demote-old in one transaction, so there's never zero or two canonicals), with best-effort `vault.json` rewrites after. No merge logic, ever.
- **When the canonical isn't connected**, the action **guides you to plug it in** (so the drives can be compared) rather than silently disabling. If the canonical is truly **lost**, a confirmed **recovery** promotes a backup as the new canonical with an **acknowledged data-loss summary** (how many at-risk photos the Mac can still supply vs are lost), then a **Mac→canonical one-way salvage sync** copies everything the Mac still holds onto the new canonical (the canonical only ever *receives* — not a merge). Truly lost = photos evicted from the Mac *and* never cloned to that backup.
- **Reappearing old canonical** — a connected drive whose `vault.json` says canonical but which isn't the registered one is detected and resolved by a confirmed **demote-to-backup or forget**; exactly-one-canonical always converges.
- **Migration to a new/bigger drive = composition**: add it → clone/Update canonical → it (until it's an exact copy) → promote it → the old demotes to backup (or forget). No new copy engine.
- **Fresh-Mac adoption** (5b): import the drive's catalog snapshot as the starting live catalog, then verify against the manifest.
- *Deferred:* migrating the canonical onto **the Mac itself** (the Mac is role `local` — a different model).

### 5.6 Drift & integrity
Every plug-in runs fast reconciliation (manifest vs. filesystem, size/mtime). Outside changes to a drive surface as a **drift review** — repair options, never auto-resolution. On-demand **Verify Integrity** re-hashes a vault against its manifest (bit-rot detection). Local vaults are watched live via FSEvents plus a verifying scan at launch.

## 6. Intelligence pipeline (all local)

One derivation pipeline, run as resumable background jobs when an asset enters the library (so derived data outlives eviction):

1. EXIF/dates/GPS/camera extraction → catalog
2. Thumbnails (content-addressed, multiple sizes)
3. Face detection + feature-print embedding (Vision)
4. OCR (`VNRecognizeTextRequest`) → FTS5
5. Semantic image embedding (CLIP-class Core ML model, e.g. MobileCLIP)
6. Reverse geocoding via a **bundled offline dataset** (no network calls; CLGeocoder rejected as online)

Backfill estimate for current library on M4 Pro: hashing minutes; thumbnails ~1–2 h; ML overnight; all interruptible/incremental.

**Faces & people:** embeddings clustered with an adjustable threshold. People view: name a cluster → Person; merge/split; per-face confidence shown; reassign individual faces. Named/confirmed assignments are written to sidecars as MWG face regions and act as fixed anchors on re-cluster — the machine never undoes a human decision. Unnamed clusters stay catalog-only.

**Search:** one box fanning out to (a) structured filters — date, folder, person, place, camera, rating, tags; (b) OCR full-text; (c) semantic vector similarity (brute-force at this scale). "Me and my girlfriend at a restaurant in Taipei" = person filters + geocoded place + semantic match. V1 uses a deterministic query parser; an on-device LLM query-rewriter is a phase-5 add-on that slots in front without changing the indexes.

**Map:** MapKit, clustered pins from the GPS index; region-select → grid.

## 7. App structure

- **`OpenPhotoCore`** — headless Swift package: vaults, manifests, hashing, catalog (GRDB), scanner/watcher, sync engine, pipeline, search. No UI imports; fully testable headless. A future `openphoto` CLI wraps this.
- **`OpenPhoto.app`** — SwiftUI. Views call Core's API; never the filesystem.

**UI surfaces** (structure; visual design comes from a separate Claude Design pass): sidebar (Timeline · Folders tree · People · Map · Bin · Devices-when-connected · pipeline status) · virtualized timeline grouped day/month/year with video/Live/offline badges · folder tree mirroring disk exactly · inspector (EXIF read-only; tags/rating/caption/people editable; presence per drive) · import grid · sync plan review · dedup review (identical-hash groups) · viewer (zoom, Live Photo playback, video).

**UI reference:** the authoritative visual design is the Claude Design handoff in `UI-Design/design_handoff_openphoto/` (interactive prototype: `OpenPhoto.html`; tokens/screens documented in its `README.md`). Recreate natively in SwiftUI — never embed the HTML. Deltas where this spec overrides the mockup: (a) bin holds items indefinitely until manually emptied — the mockup's "30 days" copy is wrong (auto-empty may become an opt-in setting later); (b) the "Albums" sidebar section is out of scope — folders, People, and Map are the organizational surfaces; (c) the sync screen's "Schedule for later" button is dropped.

**Known UI issues (to polish, surfaced 2026-06-09 — FIXED in Phase 3.5):** the vertical divider between the left sidebar and the content area didn't reach the top of the window (it stopped below the title-bar / traffic-light region). Resolved by extending it through the top safe area.

## Changelog

- **2026-06-07** — Initial approved design. Added UI reference section after Claude Design handoff landed in `UI-Design/`, with three mockup-vs-spec deltas resolved in the spec's favor.
- **2026-06-08** — Phase 1 implemented (45+ commits on `phase1-browse`). ICC deletion spike resolved §11's iPhone-deletion risk positively (works with iCloud Photos ON). Hash algorithm pinned to SHA-256 (`sha256:` prefix) in format v1 — see format doc §2. Timeline gained grouping modes (day/week/month/year/continuous) and the viewer a rename action, both from first user-testing feedback.
- **2026-06-08** — Phase 2 (Device Import) implemented and user-validated (iPhone via ImageCaptureCore, SD/volume, import registry §12, opt-in free-up-phone). Added a **Library Selection / Evict / Send-to-device / Locations** feature on top — its own design spec (`2026-06-08-library-selection-evict-send-design.md`): shared multi-select in timeline + folders, evict-to-bin with an only-copy warning, send back to an iPhone via **AirDrop** (verified by re-enumeration) and to volumes by hash-verified copy (new registries: `sends.jsonl` §13, `devices.jsonl` §14), and an inspector **Locations** panel (confirmed/believed/historical presence). Key finding: **USB push to an iPhone is impossible** (ImageCaptureCore is read+delete only) — AirDrop is the transport, the cable is identity/verification only; two spikes proved AirDrop lands photos at their original date and round-trips byte-for-byte. **Deferred to Phase 5:** a tiled thumbnail renderer to fix the dense-grid Space-switch compositing lag (`docs/spikes/2026-06-08-dense-grid-compositing-lag.md`). **Outstanding:** SD-card *send* hardware smoke. Next up: **Phase 3 (Drives)**.

- **2026-06-09** — Phase 3 (Drives) started on `phase3-drives`. **Slice 1 (sync spine)** implemented: a `DriveVolume` abstraction (one path-based `FileSystemVolume` covers folder, attached exFAT `.dmg`, and real volume — no security-scoped bookmarks, matching the app's path-based model); `SyncEngine` `plan`/`apply` for additive one-way Mac→canonical sync (atomic temp→fsync→rename, re-hash verify, resumable, **never overwrites** — collisions reported as conflicts); catalog `vault_presence` table (migration v2, isolated from `instances` so timeline browse never double-counts); drive vaults surface as `.confirmed` locations in `PresenceService`; Drives sidebar + Add-Drive adoption + Sync plan-preview sheet (free-space check) + "backed up on canonical" badge. Tested across the three-tier ladder (temp-dir scenarios + a real attached exFAT disk image — which caught an exFAT `volumeAvailableCapacityForImportantUsage == 0` free-space bug). Detailed plan: `docs/superpowers/plans/2026-06-09-phase3-slice1-sync-spine.md`. Remaining Phase 3 slices (drift/integrity, deletion propagation, evict/rehydrate + optional send-from-drive, clone/migration) each get their own spec → plan → build. Slice 1 introduces no new on-disk format.

- **2026-06-09** — Phase 3 **Slice 2 (Drift & Integrity)** implemented: `DriftReconciler` fast scan (unknown/missing/changed via size+mtime) + **Verify Integrity** (full re-hash → corrupt/bit-rot); recoverability via `PresenceService` (restorable-from / lost-no-copy); non-destructive repairs (adopt/restore/acknowledge — `VerifiedCopy` extracted from `SyncEngine`); **honest presence** (badges follow verified reality); auto-scan on connect, per-drive status line, Verify progress, and bulk Adopt/Restore actions. Detailed spec/plan: `docs/superpowers/specs/2026-06-09-phase3-slice2-drift-integrity-design.md`, `docs/superpowers/plans/2026-06-09-phase3-slice2-drift-integrity.md`. Also recorded a **Phase 5 backlog** (§10) from usage feedback: video-only filter, Finder-tag interop, tag search.

- **2026-06-09** — Phase 3 **Slice 3 (Deletion propagation)** implemented on `phase3-drives`. The first destructive slice: locally-deleted photos can have their canonical-drive copies moved into the drive vault's `.openphoto/bin/` (`origin:"propagated"`), reviewed via a standalone "Review Deletions" gate or a Sync-plan section (thumbnailed, select-all, per-row restore). A dedicated Delete-only catalog queue (`pending_deletions`, migration v4) records true deletions only — eviction never propagates. Eligibility (queued ∧ no-local-instance ∧ on-drive) is computed at review time. New `DeletionPropagator` (pure `eligible` + destructive `propagate`: BinStore move + atomic manifest rewrite + presence/queue/sync-log update; idempotent, failures stay queued). Format `vault-format-v1` §8 clarified (vault bin hosts propagated deletions; `.openphoto-trash/` is removable-non-vault-only) and §9 gained the `"delete"` event. Deferred: drive-only deletion → Slice 4; backup-drive propagation → Slice 5. Spec: `docs/superpowers/specs/2026-06-09-phase3-slice3-deletion-propagation-design.md`.
- **2026-06-09** — Phase 3 **Slice 4 (Evict / Rehydrate / drive-only deletion)** implemented on `phase3-drives`. **Evict** became the *real* operation (no longer a bin shortcut): it releases a local original to the **macOS Trash** (recoverable, never a hard-delete) only after the copy is verified on a **connected** canonical drive by **re-hashing** the drive file — `.verified` mode, the default. A `.forced` override (release trusting recorded drive presence, drive may be absent) is gated behind an acknowledgment toggle and "a few clicks", not the default. Live pairs evict as a unit (both halves verify or both are refused); the local sidecar is left in place. **Rehydrate** copies evicted (drive-only) originals back from a connected drive, hash-verified (`VerifiedCopy`), via the inverse drive→local path mapping; "already local" counts as restored (idempotent). **Drive-only deletion** (deferred from Slice 3): delete a photo that exists only on the drive → straight into the drive's `.openphoto/bin/` (`origin:"user"`, no pending queue), Live pair expanded. New `LibraryService+Eviction` (`evict`/`rehydrate`, `EvictMode`/`EvictOutcome`/`RehydrateOutcome`) and `DeletionPropagator.deleteDriveOnly`; format `vault-format-v1` §9 gained the `"rehydrate"` event. Inspector shows **Evict** when the drive is connected, **Force Evict** when it isn't, and neither when the photo isn't on the Mac or isn't backed up. Spec: `docs/superpowers/specs/2026-06-09-phase3-slice4-evict-rehydrate-design.md`. Deferred: send-from-drive → Slice 4.5; clone/migration + catalog snapshot → Slice 5.
- **2026-06-10** — Phase 3 **Slice 4.5 (Send-from-drive)** implemented on `phase3-drives`. The user can now **Send** (AirDrop to a phone / copy to a volume) photos that exist only on a plugged-in canonical drive — sourced **directly from the drive**, without rehydrating to the Mac first. New pure Core resolver `LibraryService.resolveSendSources` splits a selection into `sendable` `SendItem`s (each sourced from the local file, or directly from a connected drive's file — no temp staging) and an `unreachable` remainder (drive-only items whose drive is unplugged), grouped by drive. `SendSheet` gained a **pre-flight warning** that names which photos can't be sent and which drive to connect, with **Send N** (reachable count; disabled when none) / Cancel — the all-reachable case skips the warning entirely (unchanged flow). `SendEngine`/`SendDestination`/`VolumeCopyDestination`/`AirDropDestination`/`sends.jsonl` are reused **unchanged**: the engine is source-agnostic, so hash-keyed dedup and hash-verified copy hold regardless of where the bytes came from. Deliberately **read-only on the drive** — no drive sync-log write (the send is already journaled in `sends.jsonl` + the primary vault's sync-log), keeping the drive a pure read source. **No on-disk format change.** Spec: `docs/superpowers/specs/2026-06-10-phase3-slice4.5-send-from-drive-design.md`. Remaining in Phase 3: **Slice 5 (Clone / Migration + catalog snapshot)**, then merge `phase3-drives` → `main`.
- **2026-06-10** — Phase 3 **Slice 5a (Clone + first-class backups + durable deletion)** implemented on `phase3-drives` (first of three Slice-5 sub-slices). **Clone** mirrors the canonical onto a backup drive (`SyncEngine.planClone`, identity-mapped — destination paths equal the canonical's drive-layout paths, no re-prefix), hash-verified and **diff-driven/re-runnable** (re-running copies only what's new); `apply`'s logging was generalized so a drive→drive clone logs a `"clone"` event on the destination only. **Backups are first-class durable drives** (`AppState.durableVaults` = canonical + backup): they contribute presence/browse/the "backed up" badge and are valid rehydrate/send/verified-evict sources, with the **canonical preferred** as the read source (`connectedDrivesCanonicalFirst()`). **Durable deletion propagation:** a `pending_deletions` entry now clears only once **no** drive's `vault_presence` holds the hash (`Catalog.clearPendingDeletionsWithoutPresence`), so a deletion is remembered for an unplugged backup and applies (via the per-drive Review-Deletions gate) on its next connect — nothing resurrects from a backup. **No byte caching on the Mac** — the canonical is the byte store; backups pull additions from it when both are connected (the Mac remembers only per-drive presence + deletion hashes). The Drives panel labels Canonical/Backup and offers "Make backup" / "Update backup (N)". **No on-disk format change** (`"clone"` is an existing §9 event; clone writes the already-specified `manifest.jsonl`/`vault.json`). *Deferred to 5c:* rewriting a cloned drive's on-disk `vault.json` role to `backup` (the catalog role is authoritative for single-Mac 5a; the self-description flip matters for cross-Mac adoption/migration). Spec: `docs/superpowers/specs/2026-06-10-phase3-slice5a-clone-backups-design.md`; principle in §5.4. Remaining: **5b** (catalog-snapshot write + `catalog-schema.md` + fresh-Mac adoption), **5c** (migration / role flag-flip), then merge `phase3-drives` → `main`.
- **2026-06-10** — Phase 3 **Slice 5b (Catalog snapshot + confirmed adoption)** implemented on `phase3-drives` (second Slice-5 sub-slice). A drive now carries a **disposable copy of the machine-derived index** at `<drive>/.openphoto/catalog-snapshot/` (`catalog.sqlite` via SQLite `VACUUM INTO`, `thumbs/` for only this drive's manifest hashes, `snapshot.json` header), written **atomically** (`temp dir → replaceItemAt`) at the end of each sync and clone — documented in format §7 + the new `docs/format/catalog-schema.md` (with a portability key: a reader uses only `assets` + this drive's `vault_presence`, ignores Mac-local rows, and the `manifest.jsonl` is authoritative). **Confirmed adoption** (a prompt, never silent): plugging a snapshot-carrying drive into a Mac that doesn't know it offers **Adopt**, which **imports** the snapshot (`CatalogSnapshot.import` — assets *insert-if-absent* so local human metadata is never clobbered + this-drive presence + thumbnails) for **instant drive-only browse**, then **verifies against the manifest in the background** (`CatalogSnapshot.verifyAdoption` — manifest wins: stale rows dropped, manifest-only entries added with minimal assets). The snapshot DB is opened **read-only** so adoption never writes to the drive. Drives now **self-describe their role in `vault.json`** (`Vault.writingRole`; a clone writes `backup`), closing the 5a deferral so canonical-vs-backup is unambiguous and exactly one drive reads "Canonical". The snapshot is rebuildable, never a source of truth, never merged. **No catalog migration.** Spec: `docs/superpowers/specs/2026-06-10-phase3-slice5b-catalog-snapshot-adoption-design.md`. Remaining: **5c** (canonical management & migration — designate/change the canonical, agreement-gated promotion, demote the old), then merge `phase3-drives` → `main`; **5d** (Quick View — ephemeral, trace-free drive peek; see §10 Phase 3) is planned/optional, after the merge.
- **2026-06-10** — Phase 3 **Slice 5c (Canonical management & migration)** implemented on `phase3-drives` (third Slice-5 sub-slice). The user can now **change which drive is the canonical**: "Make this the canonical" on a connected backup, gated on **exact agreement** (`canonicalAgreement` — the backup must hold *exactly* the canonical's hashes; re-verified against both `manifest.jsonl`s at promotion time), flipped **atomically in the catalog** (`Catalog.setCanonical` — promote-new + demote-old in one transaction, so there's never zero/two canonicals) with best-effort `vault.json` rewrites (`Vault.writingRole`). When the canonical is absent the action **guides plugging it in**; if it's truly lost, a confirmed **recovery** promotes a backup as the new canonical with an **acknowledged data-loss summary** (`recoveryLoss` — at-risk photos the Mac can still supply vs are lost) and then a **Mac→canonical one-way salvage sync** recovers everything the Mac still holds (no merge logic — the canonical only ever *receives* from the Mac). A **reappearing old canonical** (a connected drive whose `vault.json` says canonical but which isn't the registered one) is detected and resolved by a confirmed **demote-to-backup or forget** — exactly-one-canonical always converges. **Migration** to a new/bigger drive is pure composition: add (5b) → clone/Update (5a) → promote (5c) → old demotes to backup (or forget). **No new on-disk artifact, no catalog migration** (`setCanonical` only updates `vaults.role`). *Deferred:* migrating the canonical onto the Mac itself (role `local`). §5.5 updated. Spec: `docs/superpowers/specs/2026-06-10-phase3-slice5c-canonical-management-migration-design.md`. **Phase 3 (Drives) is now feature-complete.** Next: **Slice 5d (Quick View — ephemeral, trace-free drive peek)**, *then* merge `phase3-drives` → `main` (the user opted to land 5d before the phase merge).
- **2026-06-10** — Phase 3 **Slice 5d (Quick View)** implemented on `phase3-drives` (final Slice-5 sub-slice). Plug in a drive — or point at any folder — and **browse it without adopting**: a sandboxed, trace-free peek that writes **nothing** to the drive and **nothing persistent** on the Mac; on **Done**/eject/quit it's gone. **Two read-only backends behind one `PeekItem`/`PeekContext`** (`PeekSource.load(root:tempDir:)`): a **snapshot-carrying** OpenPhoto drive is read instantly into a **throwaway temp `Catalog`** via `CatalogSnapshot.import` (thumbnails from the snapshot cache, full-res from the drive file); a **raw folder / non-OpenPhoto drive** is lazy-walked (`mediaFiles(under:)`, skipping `.openphoto/` + hidden + non-media) with **path-derived synthetic thumb hashes** (`syntheticHash` — SHA-256 of the path string, so it's instant and a stable cache key) and thumbnails generated on scroll. The App surface is a **main-window takeover** (`AppState.peekContext` → `PeekView`): a labeled "Viewing '<source>' — temporary · not added to your library" banner, a lazy thumbnail grid (`PeekGridCell`, the `ThumbView` async pattern over the throwaway store), and a self-contained full-screen `PeekViewer` (full-res read from the source file, reusing the CALayer `ZoomableImageView`; arrow/esc nav; "Full-res isn't available" fallback when a drive is ejected). **Invariants honored:** read-only on the drive — the raw backend **never** `Vault.openOrCreate`s a folder (that would write a `vault.json`); only the snapshot backend opens a Vault, and only because `vault.json` already exists. Nothing persisted on the Mac: all peek state (temp catalog, temp thumbnail cache) lives under one temp dir, deleted on teardown (Done, or the peeked drive ejecting mid-peek via `onVolumesChanged`). Entry points: a Drives-row **"Quick View"** button, a **"Quick View Folder…"** toolbar button + File-menu command (the raw-folder entry), and a third **"Quick View"** option on the 5b unknown-drive adoption prompt (peek before committing). **No on-disk format change, no catalog migration** (a peek writes nothing). Spec: `docs/superpowers/specs/2026-06-10-phase3-slice5d-quick-view-design.md`; plan: `docs/superpowers/plans/2026-06-10-phase3-slice5d-quick-view.md`. **Phase 3 (Drives) is now COMPLETE.** Next step: merge `phase3-drives` → `main` (user-gated — not performed by this slice).
- **2026-06-10** — **Phase 3 merged to `main`** (merge commit `721264e`, pushed to origin) and the **roadmap re-planned** (§10). The single "Phase 5 backlog" had become a junk drawer mixing three kinds of work, so it was split: a new short **Phase 3.5 (Polish & hardening)** closeout pass — the open visual bugs (tile-corner/selection-ring, presence-badge clipping, sidebar-divider gap), the correctness/invariant fixes (open-only `Vault.open`, `dateLenient` fractional-seconds, multi-drive consensus repair), and two small Browse/Send adds (video-only filter, multi-device send picker) — goes **before** the Intelligence build; the three search/Map/Locations items that overlap **Phase 4** are folded into it; and the true **extras/integrations** (Finder tags, Apple Photos import, folder-org-on-send) stay in **Phase 5**. Rationale: the polish/correctness items are cheap and high-leverage, and several are latent invariant/correctness landmines best cleared before ML is layered on the same Core paths — but **time-boxed**, since Intelligence is the differentiator. Every backlog item is now tagged with its target phase. Also flagged an unbuilt original-design surface: a **library-wide dedup review** (only dedup-on-import exists today).
- **2026-06-10** — **Phase 3.5 (Polish & hardening) implemented** on `phase3.5-polish-hardening`. Centerpiece: **one `MediaTile` + one `ThumbnailImage`** (a shared async-thumbnail loader parameterized by a `@Sendable` provider closure + a stable id, preserving the perf-tuned shared-`NSCache` pattern) replace the **four** divergent tile implementations and **three** thumbnail loaders across timeline, folders, import, free-up, and Quick View (the viewer filmstrip + send-sheet thumbnails use the loader directly). This **root-cause-fixed** two real bugs — the timeline/folders tiles' sharp corners poking past the rounded selection ring, and the drive/backed-up presence badge that only rendered at small cell sizes — because every grid now shares the one correct chrome (square → outer rounded clip → ring/checkbox/badges on the rounded frame, the structure the import grid already had). The dense-grid timeline perf pattern (sync cache read, texture bucketing, select-mode-only `GeometryReader`) is preserved. **Core hardening (TDD):** `Vault.open(at:)` opens a vault **read-only** (nil if no `vault.json`, never writes); `AppState.openVault` + Quick View's snapshot backend switch to it, so read-only drive queries never mutate a passive drive. `ISO8601Millis.dateLenient` now accepts fractional seconds (it had been rejecting the app's own timestamps). **Adds:** a video-only timeline filter (a `videoOnly` predicate on the Core timeline query + a persisted toolbar toggle) and a multi-device send picker (choose the receiver when several are connected; `SendEngine`/destinations unchanged). **No on-disk format change, no catalog migration.** Spec: `docs/superpowers/specs/2026-06-10-phase3.5-polish-hardening-design.md`; plan: `docs/superpowers/plans/2026-06-10-phase3.5-polish-hardening.md`. **Next:** the carved-out **multi-drive consensus-repair** slice, then **Phase 4 (Intelligence)**.
- **2026-06-10** — **Phase 3.5 merged to `main`** (merge commit `5c4f44a`, pushed). Beyond the spec'd 3.5 work, the branch absorbed a wave of usage-driven polish: a **unified presence badge** (one glyph by priority — drive-only-on-canonical / on-a-drive-not-canonical / on-Mac-not-backed-up / nothing-when-safe — replacing the contradictory drive+checkmark pair); **folder-view toggles** — video-only and a new **include-subfolders** (recursive `Catalog.items(inDir:recursive:)`, GLOB-scoped so a sibling like `2025x` never matches `2025`, TDD); a Drives **"Updates to sync"** yellow status when the Mac holds photos a connected drive lacks (cheap catalog-only count); the **Check/Verify-Integrity sheet** top-aligned (no empty gap); **full-height folders + inspector dividers** (matching the sidebar fix); **remove a folder import source** (+ manual folder sources now persist across volume mount/unmount); and a full **video-player overhaul** — fixed the SwiftUI `VideoPlayer` crash in this SPM-built bundle by hosting AppKit's `AVPlayerView` via an `NSViewRepresentable`; added trackpad **pinch-to-zoom + drag-to-pan** (two-finger scroll stays reserved for scrubbing); **immersive full-window playback** that fills to the top edge with the top bar floating over it and the player controls resting just above a **collapsible gallery**; and a keyboard-focus re-assert so arrow-key navigation survives leaving a video. **No on-disk format change.** **Next:** the carved-out **multi-drive consensus-repair** slice, then **Phase 4 (Intelligence)**.
- **2026-06-10** — **Multi-drive consensus repair implemented** on `multi-drive-consensus-repair` (the slice carved out of 3.5). A **"Verify All Drives"** action runs full Verify across every connected durable drive at once and surfaces findings grouped by drive; **corrupt (bit-rot)** and **missing** files now get a **one-click Repair** (the per-drive Verify sheet's corrupt findings were previously report-only). Repair sources a **verified-good copy from anywhere in the connected set** — the Mac or another durable drive whose bytes hash-match the expected (canonical) hash; `VerifiedCopy` re-hashes on copy so a rotten source fails safe. Not a majority vote — **canonical-authoritative** — and notably **the canonical itself is repairable from a backup**. New Core primitive `DriftReconciler.repairCorrupt` uses **bin-then-replace** safe ordering (stage + verify a copy to a temp → only then quarantine the rotten original to the drive bin → atomically place the verified file → re-record size/mtime), so a rotten source or interruption never empties the slot (pinned by a test). **Changed** files stay manual (a possible edit). **One small on-disk format addition:** `bin.jsonl` gains `origin:"repaired"` (a quarantined bit-rotted file — its manifest line is kept and its sidecar stays at the live location; never a deletion), documented in `vault-format-v1` §8. Spec: `docs/superpowers/specs/2026-06-10-multi-drive-consensus-repair-design.md`; plan: `docs/superpowers/plans/2026-06-10-multi-drive-consensus-repair.md`. **Phase 3 (Drives) is now fully closed out — next is Phase 4 (Intelligence).**
- **2026-06-11** — **Folder reorganization** implemented on `folder-reorg` (about to merge to `main`). Drag-drop nest / create / delete of real vault folders from the Folders view. Core `VaultReorganizer` does the on-disk move (atomic `FileManager.moveItem` same-volume rename) + folder create/delete + manifest path rewrite; the moved subtree carries its per-dir `.openphoto/` sidecars for free. `folderTree()` now walks real filesystem directories (not only catalog-counted paths), so empty folders appear at count 0. Folder delete bins any media inside (`LibraryService.delete` → `BinStore`; never a hard-delete). **Drive interaction:** structural moves are **propagated immediately to connected durable drives** (running `VaultReorganizer.moveFolder` on each connected drive vault); for **offline drives** a new `pending_folder_ops` queue (catalog migration **v6**, Mac-internal table, `vaultID`/`op`/`srcRelPath`/`dstRelPath`/`createdAtMs`) stores the ops and `AppState.applyPendingFolderOps(forDriveID:driveVault:)` applies them on the drive's next connect — before the drift scan and sync — so the path-keyed sync never sees stale paths and never duplicates files. **Scope for v1:** Mac primary vault only (the Folders view edits the Mac vault; drives receive the structural changes via propagation or the queue). **No on-disk vault format change** (manifest schema unchanged; the new queue is a Mac-internal catalog table). Spec: `docs/superpowers/specs/2026-06-11-folder-reorganization-design.md`; plan: `docs/superpowers/plans/2026-06-11-folder-reorganization.md`.
- **2026-06-11** — **Phase 4 Slice 4.4 (Reverse-geocode + Map)** implemented on `phase4.4-geocode-map`. A fourth `DerivationStage` (`GeocodeStage`) offline reverse-geocodes every geotagged photo's stored GPS to a place via a **bundled GeoNames city-level dataset** (`cities15000`, CC BY 4.0): a pure `ReverseGeocoder` (1°×1° grid-bucket + haversine nearest-city lookup, ≤250 km radius, no network, no `CLGeocoder`) loaded by `GeoNamesLoader` from three tab-separated GeoNames files. The result is stored in a new rebuildable `geocode` table (catalog migration **v9**, columns: `hash` PK → `city`/`region`/`country`/`countryCode`). The derivation stage is GPS-gated (only geotagged photos enter the pending set; no-GPS and no-nearby-city write nothing but succeed) and **catalog-keyed** (reads `assets.latitude`/`longitude` — not image bytes — so drive-only assets with unplugged drives are not skipped and still geocoded). Graceful absence is total: no dataset → `isLoaded == false` → `isAvailable == false` → runner skips the stage (jobs pending, nothing fails), mirroring `EmbedStage`. GeoNames files are **gitignored**; `tools/fetch-geonames.sh` dev-fetches them; `scripts/make-app.sh` injects the `geonames/` bundle directory best-effort (no SwiftPM resource dependency — the build succeeds on a fresh checkout with no dataset). `SearchFilters.place` + a `geocode` join in `Catalog.structuredFilter` add a **place filter lane** to unified Search (country- and city-level facets from `distinctPlaces()`). The inspector shows the resolved place as a deep-link button into Search. A **MapKit Map** (`SidebarItem.map` → `MapView`) under the Library sidebar plots geotagged assets as Apple-Photos-style clustered thumbnail annotations; tap-to-zoom, region → photo grid. **Catalog-only, no sidecar write, NO vault format change** (`vault-format-v1.md` is unchanged). `catalog-schema.md` bumped to **Version 9** documenting the `geocode` table. **GeoNames CC BY 4.0 attribution:** `fetch-geonames.sh` records the attribution requirement; an in-app About/credits screen surfacing *"Place data © GeoNames (https://www.geonames.org), CC BY 4.0."* is a **follow-up** (no About screen exists yet). The requirement is documented in `catalog-schema.md` §`geocode`. Plan: `docs/superpowers/plans/2026-06-11-phase4.4-geocode-map.md`.
- **2026-06-11** — **Phase 4 Slice 4.5 (Re-verify "sent to device" Locations on connect)** implemented on `phase4.5-locations-reverify` — the final Phase-4 slice. The inspector **Locations** "sent to *device*" indicator is now honest going forward: on device/volume connect, `AppState.reverifySentToConnectedDevices()` re-enumerates each connected send target **read-only** (the existing `SendDestination.enumeratePresent()` — never `send(...)`, so nothing is written to the device) and a new **pure `SendReverifier`** reconciles its `sends.jsonl` entries against the live listing using the **existing** two-layer match (authoritative content hash for volumes, else size+capture-second `PresenceFingerprint.looselyMatches` — identical to `SendEngine.isPresent`), producing a `present`/`gone` verdict per asset. The verdict lives **only** in a rebuildable in-memory cache on `AppState` (keyed `destinationKey|hash`, re-derived on every connect), injected into `PresenceService` via a new defaulted `reverified:` parameter. A `present` send upgrades from `.believed` to `.confirmed` ("on this device, confirmed *date*"); a deleted one downgrades to a new `Location.Confidence.stale` ("sent *date* — no longer on the device"), which correctly un-masks an only-copy in `isOnlyOnThisMac`. A *thrown* enumeration (couldn't check) leaves prior verdicts — only a successful empty listing marks sends `.gone` (no false downgrade). Hooked into the same `DeviceWatcher` connect signals the drive auto-scan uses (`onVolumesChanged` for volumes/SD; a new `deviceConnected` callback for phones; once at library-open). **No catalog migration (schema stays v9), no sidecar write, no `sends.jsonl` write, NO `vault-format-v1` change, no `catalog-schema.md` change** — the verdict is machine-derived and re-derived per connect, so it never touches disk; this is the cleanest sovereignty story in Phase 4. `SendEngine`/`SendDestination`/`VolumeCopyDestination`/`AirDropDestination`/`sends.jsonl` reused **unchanged**. TDD for Core (`SendReverifier` present/gone/hash/empty + `PresenceService` confirmed/stale/believed wiring); the live re-enumeration is a deferred hardware test (added to `docs/manual-test-checklist.md`). Plan: `docs/superpowers/plans/2026-06-11-phase4.5-locations-reverify.md`. **Phase 4 (Intelligence) is now complete — all five slices done: 4.1 (derivation pipeline + OCR), 4.2 (semantic Search), 4.3 (Faces/People), 4.4 (reverse-geocode + Map), 4.5 (Locations re-verify).**
- **2026-06-11** — **Phase 4 Slice 4.3 (Faces & People)** implemented on `phase4.3-faces-people` and merged to `main`. On-device human face detection (`VNDetectFaceRectanglesRequest`) + per-face feature-print embedding stage (`VNGenerateImageFeaturePrint`, Float16 on disk / fp32 in memory); catalog migration **v8** adds `faces` (Vision rect + Float16 embedding + confidence + source + personID) and `people` (name + createdAtMs). A pure `FaceClusterer` (cosine-distance greedy agglomerative, no Vision/Core ML dependency) groups the unassigned set on demand. A **People view** (sidebar `SidebarItem.people`) provides full management: name a cluster → Person, merge two people, split a cluster, reassign individual faces, with per-face confidence shown. Every human-confirmed assignment pairs a catalog write with an `mwg-rs:Regions` write to the XMP sidecar (the durable, portable record); unnamed auto-detected faces stay catalog-only. `SearchFilters.person` + a `faces` join add a person filter lane to the unified Search. **On-disk format change:** `vault-format-v1 §5` now has the normative MWG Regions shape (per-face: `Name`, `Type=Face`, normalized `Area` center/size, `unit="normalized"`) and the Vision↔MWG coordinate convention (Vision bottom-left corner-rect ↔ MWG top-left center-point). `catalog-schema.md` bumped to **Version 8** documenting `faces`/`people`. TDD for Core tasks T1–T4 (CRUD, FaceStage, FaceClusterer, SidecarRegions round-trip). Plan: `docs/superpowers/plans/2026-06-11-phase4.3-faces-people.md`. **Remaining in Phase 4: 4.4 (Reverse-geocode + Map), 4.5 (Locations re-verify).**
- **2026-06-11** — **Phase 4 Slice 4.2 (Semantic embeddings + unified Search)** implemented on `phase4.2-semantic-search` (not yet merged; user-gated). A second `DerivationStage` (`EmbedStage`, Core ML **MobileCLIP-S2**) writes an L2-normalized vector per photo into a new rebuildable catalog table `embeddings` (**catalog migration v7**, Float16 LE, hash-keyed, model-tagged). The 4.1 background runner was generalized into a **`DerivationStage` protocol** — `OCRStage` and `EmbedStage` both conform; the runner iterates a stage registry and drains each stage's whole pending set off the main actor. A **`SemanticIndex`** (Accelerate brute-force cosine/dot over L2-normalized fp32 vectors), a pure **`SearchRanker`** (exact-text-before-semantic ordering, no ML dependency), and SQL filter/text lanes on `Catalog` (`structuredFilter`/`textMatches`/`distinctCameras`/`distinctTags`/`items(forHashes:preservingOrder:)`) power `AppState.runSearch()`. The **unified Search surface** (`SidebarItem.search` → `SearchView`) offers: a free-text box fanning out to OCR full-text, semantic similarity (top-300 by cosine, intersected with the structured set), and caption/tag matches; click-to-narrow structured filters (date range, camera, rating, favorite, video-only, tag-AND chips); results in the shared `MediaTile` grid. Inspector tag chips navigate directly into filtered Search. `reconcileEmbeddingModel` at library-open drops stale-model rows and re-queues their embed jobs. **Machine-derived → rebuildable catalog only; no vault format change** (`vault-format-v1` is unchanged; the embeddings table is documented in `docs/format/catalog-schema.md` v7 as a droppable cache). Models are gitignored; graceful-absence build (model absent → `embedImage` returns nil → stage marks a failed attempt, re-derives when a model is later bundled). Full TDD for Core (T2–T4): embeddings round-trip, upsert-replaces, reconcile, eligibility, semantic index nearest-first, pure ranker ordering, structured filter composition, tag-AND, `items(forHashes:preservingOrder:)`, distinct cameras/tags. Plan: `docs/superpowers/plans/2026-06-11-phase4.2-semantic-search.md`. **Remaining in Phase 4: Slices 4.3 (Faces/People), 4.4 (Reverse-geocode + Map), 4.5 (Locations re-verify).**
- **2026-06-10** — **Phase 4 (Intelligence) started on `phase4.1-derivation-pipeline`; Slice 4.1 (Derivation pipeline foundation + OCR) implemented.** Phase 4 was decomposed into five slices (§10): **4.1** pipeline foundation + OCR -> **4.2** semantic embeddings + unified Search -> **4.3** Faces/People -> **4.4** reverse-geocode + Map -> **4.5** Locations re-verify on connect. **Slice 4.1** builds the resumable, per-asset background **derivation pipeline** — the spine every later Phase-4 stage plugs into — and proves it end-to-end with the first ML stage, **OCR**. Catalog migration **v5** adds `derivation_jobs(hash, stage, status, attempts, updatedAtMs, PK(hash,stage))` (per-asset per-stage completion; resumable, retry-capped at 3 attempts) and an **FTS5** `ocr(hash, text)` table; new `Catalog+Derivation` queries (`pendingDerivation`/`markDerived`/`markDerivationFailed`/`derivationProgress` + `upsertOCR`/`searchOCR`), a headless `OCRStage` (on-device Vision `VNRecognizeTextRequest`, `.accurate`), and an `AppState` background `.utility` runner that — poked at library-open and after every scan/ingest — drains the **whole** pending set off-main, resolves a reachable copy (Mac or a connected drive; a drive-only asset whose drive is unplugged is skipped and retried on connect), runs OCR, and stores the text **keyed by content hash** so it outlives eviction, with a small sidebar "Analyzing N/M..." progress line. No search **UI** yet — that is Slice 4.2, which consumes `searchOCR`. **Machine-derived -> the rebuildable catalog only; no on-disk vault format change** (the new tables are documented as rebuildable caches in `docs/format/catalog-schema.md`, now at v5). Spec: `docs/superpowers/specs/2026-06-10-phase4.1-derivation-pipeline-design.md`; plan: `docs/superpowers/plans/2026-06-10-phase4.1-derivation-pipeline.md`. **NOT yet merged** (user-gated). Remaining in Phase 4: 4.2-4.5.
- **2026-06-11** — **Phase 4 extension — power-user search (DONE).** SearchFilters refactored to include/exclude sets per negatable facet; `Catalog.structuredFilter` now composes multi-person AND + exclude + presence, folder include/exclude (recursive subtree via the union's `dirPath`, so local ∪ drive-only, sibling-safe), tag AND/exclude, place OR/exclude, camera OR/exclude, kind (photo/video/live), and has-text — all parameterised, **reading existing tables only (no migration, no on-disk format change)**. UI: a persisted **Simple/Pro** toggle over one shared filter model; Pro is a tri-state include/exclude chip bar (the LLM rewriter stays deprioritised). The search box is re-framed as the content lane (semantic + OCR + caption/tag). TDD for Core (filters + every facet + composition); App build-verified. Spec/plan: `docs/superpowers/specs/2026-06-11-power-user-search-design.md`, `docs/superpowers/plans/2026-06-11-power-user-search.md`.
- **2026-06-11** — **Phase 4 (Intelligence) gains one planned extension — power-user search** (roadmap decision; the five core slices stay complete + merged — see §10.4). Prompted by weighing a hypothetical **on-device LLM query-rewriter** against the shipped search: the rewriter was **deprioritised** because MobileCLIP's text encoder already answers natural-language *appearance* queries ("dinner at a restaurant at night" works today via the semantic lane), so its only genuine adds are named-person / place / relative-date *composition* — delivered far more cheaply, deterministically, and testably by stronger structured filters. **Power-user search:** **multi-person AND** (the current `SearchFilters.person` is single-valued, so "two specific people *together*" is inexpressible), **relative-date presets** (last summer / 2019 / last 30 days — pure date math), and a **has/without-people** toggle, layered on the existing three-lane Search; no model, fully unit-testable. **Name-aware captioning was considered and shelved to Phase 5** (research-gated): the cheap `<names> — <description>` splice adds nothing (faces already attach + index the names), while the genuinely-valuable version — identity *in context* ("Sarah is laughing while Tom pours the wine") — requires a heavy multi-GB VLM to **bind a named identity to a specific action**, which off-the-shelf local VLMs do unreliably (a confident wrong binding is worse than no caption) and personalized/in-context identity grounding is still research-grade. It reopens only if (a) local personalized-VLM grounding matures and proves reliable, or (b) **accessibility / alt-text** becomes a goal (then *nameless* scene captions from a small model stand on their own). **Perceptual near-duplicate / burst culling also stays Phase 5.** The extension gets its own brainstorm → spec → plan before any build; no code yet.
- **2026-06-11** — **Phase 5 (Extras & integrations) reviewed + rescoped** (§10.5). Phase 5 is optional reach; the core is complete. **Building:** perceptual **near-duplicate / burst culling** (reuses the existing CLIP + face embeddings — the marquee item, building first), **library import** from Apple Photos / iCloud (PhotoKit, network-downloads originals — iCloud folds into this, no separate feature) **and Google Takeout** (the live Google Photos Library API was restricted in 2025 → a Takeout export folder + per-photo JSON is the robust, sovereignty-correct path), **Finder-tag interoperability** (XMP stays authoritative; Finder tags are an *additive* two-way xattr bridge), and **sidecar-layout export** (`.openphoto/` ↔ beside-file). **Cut/parked:** the **LLM query rewriter** (superseded by the deterministic power-user search; non-deterministic, off-ethos), **name-aware captioning** (research-gated), **folder-org-on-send** (infeasible over AirDrop without a companion iOS app), and the **`openphoto` CLI** (a server links headless Core directly — a CLI only serves personal scripting, not needed). First build: near-duplicate culling.
- **2026-06-11** — **Phase 5 — near-duplicate / burst culling (DONE).** A dedicated **Tidy Up** surface (`SidebarItem.tidyUp`) with two modes: **Bursts** (time-window + CLIP-cosine grouping over the existing `embeddings`, keeper = sharpest via on-demand variance-of-Laplacian on cached thumbnails) and **Duplicates** (a new 64-bit perceptual **dHash** per photo — `PHashStage` → rebuildable `phash` table, **catalog migration v10** — grouped within the **same folder**, keeper = highest resolution). Pure unit-tested groupers + keeper (`BurstGrouper`/`DuplicateGrouper`/`KeeperSelector`); favorites/rated never auto-evicted; selection keyed by `instanceID`; deletion reuses the recoverable bin path. **Catalog-only, no vault-format change** (`catalog-schema.md` → Version 10). Spec/plan: `docs/superpowers/specs/2026-06-11-near-dup-culling-design.md`, `docs/superpowers/plans/2026-06-11-near-dup-culling.md`.
- **2026-06-12** — **Phase 5 — new import sources (DONE).** Two read-only `ImportSource`s feeding the existing import grid + pipeline: **`PhotosLibrarySource`** (PhotoKit — Apple gallery + iCloud; network-downloads originals; expands edited siblings + Live-Photo still/video pairs into linked items; folds Apple's favorite into the file) and **`TakeoutSource`** (Google Takeout folder + per-photo JSON via a quirk-tolerant `TakeoutJSONMatcher`; **folds the JSON's date/GPS/description/favorite into self-describing files** through `EmbeddedMetadata` — lossless EXIF+XMP via `CGImageDestinationCopyImageSource`, no pixel recompress — and **discards the JSON**, never copying it in). The scanner now reads **embedded XMP** as a base layer (`EmbeddedMetadata.read` → reused `XMP.parse`, which also learned to accept `xmp:Rating`/`xmp:Label` in element form), with the `.openphoto/` sidecar still authoritative (**precedence: sidecar > embedded > defaults**). Both sources land photos **flat** in one chosen folder; the **Folders-screen "Move photos between folders"** organizer is the agreed next slice (§10 backlog). New sidebar entries: a permanent **Apple Photos** and a Takeout folder auto-detected via "Add import source…"; both import-only (`sendDestination → nil`, free-up gated to camera+volume); `make-app.sh` gained `NSPhotoLibraryUsageDescription`. **No on-disk / catalog format change** (`schemaVersion` untouched; `catalog-schema.md` not touched). `PhotosLibrarySource` is build-verified (PhotoKit needs hardware) — awaits Jude's smoke test; the Core fold/parse/scan paths are TDD'd. Spec/plan: `docs/superpowers/specs/2026-06-12-new-import-sources-design.md`, `docs/superpowers/plans/2026-06-12-new-import-sources.md`. Also added a small App-only banner on the iPhone import grid noting that **Live Photos flatten to stills over USB** (Apple/ImageCaptureCore doesn't expose the motion `.mov` — confirmed by the Phase-2 spike), pointing users to import via Apple Photos or AirDrop-to-a-folder to keep them live.
- **2026-06-12** — **Select & move photos shipped (Phase 5.5 slice 1)** — the Folders screen's Select mode gains move affordances: a destination dropdown + Move button (the import screen's picker pattern, incl. "New folder…" typed destinations, created implicitly by the move), and a **"Drag to Move"** toggle that flips grid dragging from rubber-band-select to dragging the selection onto a folder row (or the root header) in the left tree. Underneath: file-grain **`VaultReorganizer.moveFile`** (atomic same-volume rename + `.openphoto` sidecar travel + single manifest-entry rewrite, collision-safe via the newly shared `FileNaming`), **`LibraryService.movePhotos`** (Live-pair carry, failure aggregation), and full **drive parity** mirroring folder reorg: connected durable drives move now (exact-target keeps basenames aligned), offline drives queue **`"moveFile"`** ops in `pending_folder_ops` (TEXT op kind — **no schema change**, schemaVersion stays 11; documented in `catalog-schema.md`), drive-only items re-key their `vault_presence` row and appear in the new folder immediately. One rescan reconciles; failures aggregate into one alert; the user stays in the source folder. Spec: `2026-06-12-select-move-photos-design.md`.
- **2026-06-12** — **Foreign imports shipped (Phase 5.5 slice 2)** — importing from libraries that aren't yours. **(A) Foreign OpenPhoto drives:** a mounted volume carrying an unknown vault auto-surfaces as a read-only import source (`ForeignVaultSource`); enumeration comes from the drive's *documented* formats (manifest.jsonl inventory; catalog-snapshot dates + thumbnails read with the documented reader rules — OpenPhoto as its own first third-party snapshot implementor); their manifest hashes pre-flag "already in your library" with zero I/O; a folder panel picks their folders, which land with structure intact under a chosen parent (engine gained per-item subdirs); an "Include their metadata" toggle (default OFF) copies their `.openphoto` sidecars alongside (post-place hook, before the rescan). Adoption-as-my-drive is untouched. **(B) Friend's Apple Photos:** documented export workflow (Export Unmodified Originals + IPTC-as-XMP; transport via external drive / Thunderbolt-Bridge share / Wi-Fi share); folder import now enumerates 10k-class exports with bounded-concurrency EXIF reads + progress, and the same metadata toggle folds the exported `.xmp` sidecars into the copies (Takeout-style, before hashing). No format change; schemaVersion stays 11. Spec: `2026-06-12-foreign-imports-design.md`.
- **2026-06-12** — **Phase 5.5 roadmap set (next-steps note, no code yet)** — the planned Phase-5 "Building" items are all DONE, so Jude set the forward order for the next working session (recorded in §10.5 "Phase 5.5"): **(1) Select & move photos — Folders screen** (the next build; a Folders-screen organize mode — drag to multi-select, then move the selection two ways: drag it onto a folder in the left tree, or pick a destination from a folder dropdown like the import picker; built on a new per-photo atomic move primitive — full design in the "Move photos between folders" backlog item); **(2) Import from other people's Apple Photos/iCloud + their OpenPhoto drives** (per-folder; needs a short ingress brainstorm); **(3) Tile-grid rendering optimization** (the deferred dense-grid tiled renderer for the min-zoom lag). *(Amended later 2026-06-12: an **undo stack** item — ⌘Z for deletes/renames/photo- and folder-moves via inverse ops — was inserted as item 3, pushing the optimization pass to item 4.)* Also clarified that **library dedup is considered covered** by Tidy Up's Duplicates mode — the old "library-wide exact-hash" backlog item is largely superseded (cross-folder identical copies are usually intentional) and flagged likely-drop.
- **2026-06-11** — **Phase 5 — metadata interop + Settings (DONE).** **Sidecar export:** a File-menu "Export Metadata Sidecars…" writes a portable mirror tree of standard `.xmp` (one per tagged asset, `dest/<relPath>.xmp`) to a chosen folder — the library is never touched. **Finder-tag two-way sync (opt-in):** OpenPhoto tags and macOS Finder tags stay identical via a pure 3-way `TagMerge` against a per-photo baseline (rebuildable `finder_tag_sync` table, **catalog migration v11**), so removals on either side propagate; XMP stays authoritative; off by default (writing Finder xattrs to originals is non-destructive — content hash unchanged — but gated on opt-in). **Settings window** (native ⌘, scene): the Finder-sync toggle + "Sync now", and the **GeoNames CC BY 4.0 attribution** owed from the Map (the 4.4 follow-up, now satisfied). **Catalog-only, no vault-format change** (`catalog-schema.md` → Version 11). Spec/plan: `docs/superpowers/specs/2026-06-11-metadata-interop-design.md`, `docs/superpowers/plans/2026-06-11-metadata-interop.md`.

## 8. Error-handling doctrine

1. Originals never written; every mutation is a new file in a `.openphoto/` dir
2. All writes atomic (temp → fsync → rename); all copies hash-verified before being recorded
3. Ambiguity becomes a human review, never an auto-resolution
4. Every long job resumable; sync re-runs idempotent
5. Catalog rebuildable from vaults; manifests rebuildable from filesystem + catalog — no single point of data death
6. Plans pre-check resources and fail before acting

Permissions: Full Disk Access for `~/Pictures`/`~/Movies` if sandboxed-out; security-scoped bookmarks for drive vaults.

## 9. Testing

- **Core unit tests:** hashing, manifest round-trip, sidecar emit/parse, Live Photo pairing, dedup.
- **Sync scenario tests** on throwaway temp-dir vaults: kill-mid-copy resume, double-run idempotency, drift injection, rename storms, evict-safety (refusal without verified canonical copy).
- **Pipeline golden tests:** fixture images with known faces, OCR text, GPS.
- UI: manual for v1.

## 10. Phases

Each phase is its own implementation plan and ends with a usable app:

1. **Browse** ✅ — Core foundation (vault/manifest/hash/catalog/scanner), timeline, folder tree, viewer, inspector, local bin. *Includes the ImageCaptureCore deletion spike to de-risk phase 2.*
2. **Import** ✅ — iPhone/SD grid, verify-then-delete, dedup-on-import. *Plus the out-of-band Library Selection / Evict / Send-to-device / Locations feature (see changelog).*
3. **Drives** ✅ COMPLETE — canonical sync (**Slice 1**: additive Mac→canonical sync spine), evict/rehydrate, clone/migrate, drift review, integrity verify, delete propagation, catalog snapshot, ephemeral peek. *Decomposed into slices; see the 2026-06-09/06-10 changelog entries and `docs/superpowers/specs/2026-06-09-phase3-drives-design.md`.* Slices done: 1 (sync), 2 (drift/integrity), 3 (deletion propagation), 4 (evict/rehydrate), 4.5 (send-from-drive), 5a (clone + durable backups), 5b (catalog snapshot + adoption), 5c (canonical management & migration), 5d (Quick View). **Merged to `main` 2026-06-10** (merge commit `721264e`).
   - **Slice 5d (DONE): Quick View (ephemeral, trace-free drive/folder peek).** Plug a drive in — or point at any folder — and **browse it without adopting** (a sandboxed peek; on **Done**/eject nothing persists). Two read-only backends behind one `PeekItem`/`PeekContext` (`PeekSource.load`): a snapshot-carrying OpenPhoto drive is read instantly into a **throwaway temp `Catalog`** via `CatalogSnapshot.import` (thumbnails from the snapshot, full-res from the drive file); a raw folder / non-OpenPhoto drive is lazy-walked with path-derived synthetic thumb hashes (thumbnails generated on scroll). Main-window-takeover `PeekView` (banner + grid + a self-contained full-screen `PeekViewer` reusing `ZoomableImageView`). **Read-only on the drive** (the raw backend never `openOrCreate`s a folder — that would write a `vault.json`); **nothing persisted on the Mac** (all peek state under one temp dir, deleted on teardown). Entry points: Drives-row "Quick View", "Quick View Folder…" (toolbar + File menu), and the Adopt/**Quick View**/Not-now adoption prompt. Spec: `docs/superpowers/specs/2026-06-10-phase3-slice5d-quick-view-design.md`.
3.5. **Polish & hardening (DONE)** — closeout pass over the shipped phases (1–3). **Unified all photo tiles** into one `MediaTile` + a shared `ThumbnailImage` (a `@Sendable`-provider async loader) across timeline, folders, import, free-up, and Quick View — plus the viewer filmstrip and send-sheet thumbnails — which **root-cause-fixed** the timeline/folders sharp-corner-vs-rounded-ring poke-out and the presence badge that only rendered at small cell sizes (all four grids now share the one correct chrome — square → outer rounded clip → ring/checkbox/badges on the rounded frame — and the dense-grid perf pattern is preserved). Full-height sidebar divider. **Core hardening (TDD):** open-only `Vault.open` so read-only drive queries (incl. Quick View's snapshot backend) never write a `vault.json` to a passive drive; `ISO8601Millis.dateLenient` now parses the app's own fractional-second timestamps. **Adds:** a video-only timeline filter (a `videoOnly` Core query predicate + a toolbar toggle) and a multi-device send picker. *Multi-drive consensus repair was **carved out** to its own slice — it's a real Verify-Integrity feature, not a papercut.* **Merged to `main` 2026-06-10** (`5c4f44a`). The branch also absorbed a wave of usage-driven polish (see the 2026-06-10 changelog): a unified presence badge; folder video-only + include-subfolders toggles; a Drives "Updates to sync" status; full-height folders/inspector dividers; remove-folder-import-source; and a full video-player overhaul (crash fix, pinch-zoom + drag-pan, immersive full-window playback, collapsible gallery). Spec/plan: `docs/superpowers/specs/2026-06-10-phase3.5-polish-hardening-design.md`. **The carved-out multi-drive consensus-repair slice is now also done (2026-06-10) — Phase 3 (Drives) is fully closed out; next is Phase 4 (Intelligence).**
4. **Intelligence** ✅ COMPLETE — the headline phase: the all-local derivation pipeline (EXIF/dates/GPS/camera, thumbnails, face detection + embeddings, OCR→FTS5, CLIP-class semantic embeddings, offline reverse-geocode), **People** view, **unified search**, and **Map**. **Folds in** the backlog items that are really part of this work, not extras: tag search (part of unified search), the **Map** under the Library sidebar (it *is* this phase's Map), and re-verifying "sent to device" Locations on connect (Locations/People honesty). Items tagged **[Phase 4]** below.
   - **Slices (decomposed 2026-06-10):** **4.1 — Derivation pipeline foundation + OCR (DONE)** · **4.2 — Semantic image embeddings + unified Search (DONE)** · **4.3 — Faces → People (DONE)** · **4.4 — Reverse-geocode + Map (DONE)** · **4.5 — re-verify "sent to device" Locations on connect (DONE)**. Build order: 4.1 -> 4.2 -> 4.3 -> 4.4 -> 4.5. **Phase 4 (Intelligence) is now complete — all five slices done.**
   - **Slice 4.1 (DONE): Derivation pipeline foundation + OCR.** A resumable, per-asset background **derivation pipeline** — the spine every later Phase-4 stage plugs into — proven end-to-end with the first ML stage, **OCR**. Catalog migration **v5** adds `derivation_jobs(hash, stage, status, attempts, updatedAtMs, PK(hash,stage))` (per-asset per-stage completion; resumable, retry-capped) plus an FTS5 `ocr(hash, text)` table; the `Catalog+Derivation` queries (`pendingDerivation`/`markDerived`/`markDerivationFailed`/`derivationProgress` + `upsertOCR`/`searchOCR`); a headless `OCRStage` (on-device Vision `VNRecognizeTextRequest`); and an `AppState` background `.utility` runner (poked at library-open and after every scan/ingest) that drains the whole pending set off-main, resolves a reachable copy (Mac or a connected drive — a drive-only asset whose drive is unplugged is skipped and retried on connect), and stores OCR text keyed by **content hash** so it outlives eviction, with a small sidebar "Analyzing N/M..." progress line. **Machine-derived -> the rebuildable catalog only; no on-disk vault format change.** No search *UI* yet (that is Slice 4.2, which consumes `searchOCR`). Spec: `docs/superpowers/specs/2026-06-10-phase4.1-derivation-pipeline-design.md`.
   - **Slice 4.2 (DONE): Semantic image embeddings + unified Search.** A second `DerivationStage` (`EmbedStage`, Core ML) produces an L2-normalized MobileCLIP-S2 vector per photo and writes it into a new rebuildable catalog table `embeddings` (catalog migration **v7**, Float16 LE, hash-keyed, model-tagged for safe invalidation). The 4.1 runner was generalized into a **`DerivationStage` protocol** — `OCRStage` and `EmbedStage` both conform; the runner iterates the stage registry and drains each stage's whole pending set off-main. A **`SemanticIndex`** (Accelerate brute-force cosine — dot product over L2-normalized vectors) + a pure **`SearchRanker`** + SQL filter/text lanes on `Catalog` power `AppState.runSearch()`, rendered in the existing `MediaTile` grid under a new `SidebarItem.search`. The **unified Search box** fans out to: structured click-to-narrow filters (date range, camera, rating, favorite, video-only, tag-AND), OCR full-text (`searchOCR`), and semantic similarity (top-300 by score, intersected with the structured set). Inspector tag chips navigate directly into filtered Search. **Machine-derived → rebuildable catalog only; no vault format change; models gitignored + graceful-absence build** (model not bundled = `embedImage` returns nil → stage marks failed-attempt, re-derives when a model is later bundled). Catalog schema now at **v7**. Implemented on `phase4.2-semantic-search`. Spec: `docs/superpowers/plans/2026-06-11-phase4.2-semantic-search.md`. Remaining in Phase 4: **4.3–4.5**.
   - **Slice 4.3 (DONE): Faces → People (full management).** A third `DerivationStage` (`FaceDerivationStage`, Apple Vision) detects human faces in every photo (`VNDetectFaceRectanglesRequest`), crops each face bounding box, and runs `VNGenerateImageFeaturePrint` to produce a per-face Float16 feature-print embedding, written into two new rebuildable catalog tables (`faces`/`people`, catalog migration **v8**). `faces` stores: normalized Vision bounding rect (`rectX`/`rectY`/`rectW`/`rectH`, bottom-left origin), Float16 embedding blob + `dim`, `confidence`, `source` (`"auto"` | `"confirmed"`), and `personID` (NULL = unassigned). `people` stores: `name` + `createdAtMs`. A **pure `FaceClusterer`** (cosine-distance greedy agglomerative, no Vision/Core ML dependency — the thoroughly-tested heart) groups the unassigned set on demand. A **People view** in the app sidebar (`SidebarItem.people`) surfaces named people and suggested clusters; the user can name a cluster → Person, merge two people, split a cluster, and reassign individual faces — every confirmed assignment pairs a catalog write with a **`mwg-rs:Regions` write to the asset's XMP sidecar** (the durable, portable record). `SearchFilters.person` + a `faces` join in `Catalog.structuredFilter` add a person lane to the unified Search. **Sovereignty split:** `source = 'auto'` rows (rects, embeddings, confidence, unnamed clusters) are machine-derived / rebuildable-cache-only; `source = 'confirmed'` rows and `people.name` mirror human decisions recorded durably in sidecars — the machine never overwrites confirmed rows (`replaceFaces` deletes only `source = 'auto'`). **On-disk format change:** `vault-format-v1.md §5` now documents the normative MWG Regions shape (per-face: `Name`, `Type=Face`, normalized `Area` center/size, top-left origin) and the Vision↔MWG coordinate conversion (Vision bottom-left corner-rect ↔ MWG top-left center-point). Catalog schema now at **v8**. Implemented on `phase4.3-faces-people`. Plan: `docs/superpowers/plans/2026-06-11-phase4.3-faces-people.md`. Remaining in Phase 4: **4.4–4.5**.
   - **Slice 4.4 (DONE): Reverse-geocode + Map.** A fourth `DerivationStage` (`GeocodeStage`) offline reverse-geocodes every geotagged photo's stored GPS to a city/region/country using a **bundled GeoNames city-level dataset** (`cities15000`, CC BY 4.0) — pure `ReverseGeocoder` (1°×1° grid-bucket + haversine nearest-city, ≤250 km radius, no network, no `CLGeocoder`). Derivation result is written into a new rebuildable catalog table `geocode` (hash PK → `city`/`region`/`country`/`countryCode`, catalog migration **v9**). The stage is **GPS-gated**: only assets with non-null `latitude`/`longitude` enter the pending set. A photo on an unplugged drive (no local file) is still geocoded because the stage reads the catalog's stored GPS, not the image bytes — drive-only assets are **exempt** from the per-item reachable-URL skip. Graceful absence is total: no bundled dataset → `isLoaded == false` → `isAvailable == false` → the runner skips the stage (jobs stay pending, nothing fails), exactly mirroring `EmbedStage`. The GeoNames files are **gitignored** and dev-fetched via `tools/fetch-geonames.sh`; `scripts/make-app.sh` injects the `geonames/` directory into the `.app` bundle best-effort (silent/no-op if absent — the build has **no SwiftPM resource dependency**). `SearchFilters.place` + a `geocode` join in `Catalog.structuredFilter` add a place lane to the unified Search (country- and city-level facets via `distinctPlaces()`). The inspector shows the resolved place as a deep-link button that narrows Search to that place. A new **Map** (`SidebarItem.map` → `MapView`) under the Library sidebar group plots geotagged assets as Apple-Photos-style **clustered thumbnail annotations** on a MapKit `Map`; tap-to-zoom, region → photo grid. **Catalog-only, no sidecar write, NO vault format change** (`vault-format-v1.md` is unchanged). Catalog schema now at **v9**. Implemented on `phase4.4-geocode-map`. Plan: `docs/superpowers/plans/2026-06-11-phase4.4-geocode-map.md`. Remaining in Phase 4: **none — Slice 4.5 done; Phase 4 complete.**
   - **Slice 4.5 (DONE): Re-verify "sent to device" Locations on connect.** When a device (iPhone via ImageCaptureCore / SD card / volume) connects, OpenPhoto re-enumerates it **read-only** (the existing `SendDestination.enumeratePresent()` send-verification path — never `send(...)`) and a pure `SendReverifier` reconciles its `sends.jsonl` entries against the current listing via the existing two-layer match (authoritative content hash for volumes, else size+capture-second `PresenceFingerprint.looselyMatches` — identical to `SendEngine.isPresent`), yielding `present`/`gone` per asset. The verdict lives **only** in a rebuildable in-memory cache on `AppState` (keyed `destinationKey|hash`, re-derived every connect), injected into `PresenceService.locations(forHash:)`: a still-present send upgrades from `.believed` to **`.confirmed`** ("on this device, confirmed *date*"), a deleted one downgrades to a new **`.stale`** confidence ("sent *date* — no longer on the device") — which correctly un-masks an only-copy in `isOnlyOnThisMac`. Hooked into the same `DeviceWatcher` connect signals the drive auto-scan uses (`onVolumesChanged` for volumes/SD; a `deviceConnected` callback for phones; once at library-open). **Never writes to the device; in-memory rebuildable verdict cache → no catalog migration (schema stays v9); no sidecar; no `sends.jsonl` write; NO on-disk format change** — the cleanest sovereignty story in Phase 4. `SendEngine`/`SendDestination`/`VolumeCopyDestination`/`AirDropDestination`/`sends.jsonl` reused **unchanged**. Pure matcher TDD'd on synthetic data; the live re-enumeration is a **deferred hardware test** (added to `docs/manual-test-checklist.md`). Implemented on `phase4.5-locations-reverify`. Plan: `docs/superpowers/plans/2026-06-11-phase4.5-locations-reverify.md`.
   - **Extension — power-user search (DONE 2026-06-11).** Strengthen the existing three-lane Search with deterministic structured filters rather than an on-device LLM rewriter (deprioritised — see the 2026-06-11 changelog: MobileCLIP's text encoder already covers natural-language *appearance* queries, so the rewriter's only real adds are named-person/place/relative-date composition, bought far more cheaply and testably here). Concrete gaps to close: **multi-person AND** (today's `SearchFilters.person` is single-valued → "X and Y *together*" is currently inexpressible), **relative-date presets** ("last summer", "2019", last 30 days — pure date math), and a **has/without-people** toggle. No model, fully unit-testable, instant.
     - *Considered and **shelved to Phase 5** (§10.5): **name-aware captioning** — the cosmetic `<names> — <description>` splice adds nothing (faces already attach + index the names), while the genuinely-valuable identity-*in-context* version ("Sarah is laughing while Tom pours the wine") needs a heavy local VLM to bind a named identity to a specific action, which off-the-shelf local VLMs do unreliably (a confident wrong binding is worse than no caption) and personalized/in-context grounding is still research-grade; reopens only if that tech matures or **accessibility/alt-text** becomes a goal (then *nameless* captions stand alone) — and **perceptual near-duplicate / burst culling**.*
5. **Extras & integrations** — optional reach beyond the core promise (Browse/Import/Drives/Intelligence are complete). **Scope set by the 2026-06-11 review (see changelog).** Detailed technical notes for the building items live in the backlog below.
   - **Building:**
     1. **Perceptual near-duplicate / burst culling (DONE 2026-06-11).** — group visually near-identical shots (bursts, slight variations) **reusing the CLIP image embeddings + face vectors already computed** (no new model), so the user can keep the best and evict the rest. The marquee Phase-5 item — directly serves "own your files, reclaim space"; distinct from the exact-hash dedup-on-import that exists today. Done — see changelog entry 2026-06-11 and `docs/superpowers/specs/2026-06-11-near-dup-culling-design.md`.
     2. **Library import — Apple Photos / iCloud + Google Takeout (DONE 2026-06-12).** A new `ImportSource` reading the Mac's **Apple Photos** library via **PhotoKit** (`PHAsset` → `PHAssetResourceManager`, originals only, never writing back); with network access this also pulls **iCloud** originals down, so "iCloud import" is the *same* feature (limited to the iCloud account the Mac is signed into — no arbitrary-account web path). **Google Photos via Google Takeout, NOT the live API** (Google restricted the Photos Library API in 2025 — third-party apps can no longer enumerate a whole library, so bulk migration is gone): point at a Takeout export folder and parse its per-photo JSON sidecars to recover timestamps/GPS, feeding the existing copy→hash→dedup→place pipeline. Sovereignty-correct: import only ever pulls data *out* into plain local files. Done — both sources land photos **flat** (organize-later is the next slice, §10 backlog) and the Takeout JSON is **folded into self-describing files** (standard EXIF+XMP, JSON discarded), with the scanner reading embedded XMP. See changelog 2026-06-12 and `docs/superpowers/specs/2026-06-12-new-import-sources-design.md`.
     3. **Finder-tag interoperability (DONE 2026-06-11).** — XMP sidecars (`dc:subject`) stay the **authoritative, portable** tag record; Finder tags (the `com.apple.metadata:_kMDItemUserTags` xattr) are an **additive two-way bridge** (OpenPhoto tags become Finder-visible; tags added in Finder are read back). Both coexist; XMP is never demoted to Finder-only.
     4. **Sidecar-layout export (DONE 2026-06-11).** — convert sidecar placement between the hidden per-folder `.openphoto/` layout and **beside-file** (`photo.xmp` next to `photo.jpg`) for interop with Lightroom / other XMP-aware tools.
   - **Cut / parked (2026-06-11 review):**
     - **LLM query rewriter** — *cut.* The deterministic power-user search (Phase 4 extension) already delivers its value; an on-device rewriter is non-deterministic (against the testable-Core ethos) and over-engineered for the marginal gain.
     - **Name-aware captioning** — *parked (research-gated).* The valuable identity-in-context version needs reliable local personalized-VLM grounding that isn't deployable; reopens only if that matures or accessibility/alt-text becomes a goal.
     - **Folder-organization preservation when sending to a phone** — *cut.* iOS has flat albums and AirDrop can't create/assign them, so it's infeasible without shipping a companion iOS app (scope creep, off-mission).
     - **`openphoto` CLI** — *cut.* A future server links headless `OpenPhotoCore` **directly**; a CLI helps only personal terminal scripting, for which there's no current need.
   - **Phase 5.5 — next steps, IN THIS ORDER (recorded 2026-06-12 from Jude's direction; the planned Phase-5 "Building" items above are all DONE).** This is the forward roadmap for the next working session:
     1. **DONE (2026-06-12).** **Select & move photos — Folders screen** (the important one; full design in the backlog item below "Move photos between folders"). A Folders-screen organize mode: drag to multi-select photos, then move the selection **two ways** — drag it onto a folder in the left folder tree, OR pick a destination from a folder dropdown (like the import screen's picker). Built on a new per-photo atomic move primitive. *This completes the import→organize loop and is the agreed next build.*
     2. **DONE (2026-06-12).** **Import from other people's libraries** (backlog item "Import from *other people's* libraries" below) — their Apple Photos / iCloud and their OpenPhoto drives (per-folder). Needs a short brainstorm on the sovereignty-correct ingress before building.
     3. **Undo stack** (added 2026-06-12, Jude) — session-scoped undo (⌘Z) for the structural ops: deletes (bin moves), renames, per-photo moves between folders, folder moves/renames. Every one of these is already non-destructive and mechanically reversible (bin restore, rename back, move back — nothing hard-deletes, invariant #3), so undo = record the inverse op and apply it through the same machinery, drive propagation included. Needs a short design pass first (in-memory per-session vs persisted; interaction with the offline-drive op queues; how far the stack reaches).
     4. **Tile-grid rendering optimization** — the deferred **dense-grid tiled renderer** that fixes the min-zoom Space-switch compositing lag (root-caused: per-image GPU textures; spike `docs/spikes/2026-06-08-dense-grid-compositing-lag.md`). A focused perf pass, done after the features above.
     - *Dedup is considered covered:* Tidy Up's **Duplicates** mode (perceptual dHash, same-folder) handles real duplicate culling; the old "library-wide exact-hash" backlog item is largely superseded — see its annotation below.

### Backlog, re-filed into phases (originally one bucket; re-filed 2026-06-10)

Each item is tagged with the phase it now belongs to: **[3.5]** polish/hardening closeout · **[Phase 4]** part of Intelligence · **[Phase 5]** extras/integrations. (Re-filed from the single "Phase 5 backlog" — see the 2026-06-10 changelog for the rationale.)

> **Status (2026-06-10):** Phase 3.5 is **merged to `main`** (`5c4f44a`). All **[3.5]** items are **done** — video-only filter, photo-tile corner shape, drive/presence badge, open-only `Vault.open`, multi-device send picker, `dateLenient` fractional-seconds — including **multi-drive consensus repair**, which was carved out into its own slice and is now **also done** (2026-06-10) — so **Phase 3 (Drives) is fully closed out**. The **[Phase 4]** / **[Phase 5]** items remain for those phases. (A larger wave of usage-driven polish also landed on the 3.5 branch — folder toggles, drive sync-status, dividers, remove-import-source, and the video-player overhaul — recorded in the 2026-06-10 changelog rather than re-listed here.)

- **[3.5] Video-only filter** — a quick filter in the timeline (and folder view) to show only videos. Small Browse-layer addition; complements the existing photo/video split already tracked per asset (`MediaKind`).
- **[Phase 5] macOS Finder tag interoperability** — make OpenPhoto's tags correspond to macOS Finder tags, not only the XMP tags it uses today. Two directions: **read** tags a user added in Finder, and **write** OpenPhoto tags so they're readable in Finder. *Technical note for whoever designs this:* OpenPhoto currently stores tags as XMP `dc:subject` in sidecars. Finder tags are a different mechanism — they live in the extended attribute `com.apple.metadata:_kMDItemUserTags` (surfaced via Spotlight's `kMDItemUserTags`), not in XMP. So this needs a design decision: bridge/sync the two, or treat Finder tags as a first-class source. Watch the sovereignty invariant — Finder tags are an OS-managed xattr, so syncing them to/from the XMP sidecars (which remain the portable record) is the likely approach.
- **[Phase 4] Tag search in timeline & folder views** — search/filter assets by tag in both browse surfaces. *Overlap note:* this is closely related to Phase 4's unified search (Intelligence) — a basic tag-only search could land in **Phase 4** alongside search rather than waiting for Phase 5; worth deciding when Phase 4 is planned.
- **[Phase 4] Map in the Library sidebar** — a **Map** entry in the left navbar's Library group (alongside Timeline / Folders / Drives / Bin) that plots geotagged photos. *Overlap note:* a Map view is already slated for **Phase 4 (Intelligence)** as part of the pipeline/search work — this records it as a concrete sidebar surface under Library; reconcile the two when Phase 4 is planned (it may simply be the Phase 4 Map, confirmed to live under Library).
- **[3.5] Unify photo-tile corner shape across surfaces** (surfaced 2026-06-09, from usage feedback) — the import-screen grid uses **rounded** thumbnail tiles while the timeline (and folder) grid uses **sharp** corners; the sharp corners read as inconsistent and, worse, when a photo is selected the square photo corners **poke out past the rounded selection ring**, looking broken. Unify on the rounded tile shape everywhere (timeline, folders, import), and ensure the thumbnail is clipped to the same corner radius as — or slightly inside — the selection ring so no corner protrudes. Browse/Selection-layer styling change (`ThumbView`/`PhotoCellView` + `selectionChrome` corner radii); no data or format impact.
- **[3.5] Drive/presence indicator on photo tiles — timing + clipping** (surfaced 2026-06-09, from usage feedback) — the per-tile presence badge (the "on drive" / drive-only / backed-up glyph in `PhotoCellView`) appears to (a) **get clipped at the tile edge** — it's drawn past the thumbnail's clip bounds, so part of the glyph is cut off (likely the same corner-clipping issue as the tile-shape item above — fix together), and (b) possibly **not show at the right time** — verify the badge's appearance is driven by *honest* presence (it should reflect actual drive-only vs backed-up-on-canonical vs local-only state, refreshed on the same signals as the Drives badges). Audit when the glyph is shown vs the asset's true presence, and inset it within the rounded tile so it's never clipped. Browse-layer fix; no data/format impact.
- **[Phase 5] Apple Photos library as an import source** (surfaced 2026-06-09, from usage feedback) — let users import originals from the Mac's **Apple Photos** library (the managed `.photoslibrary` bundle), alongside the existing phone/SD/folder sources. *Technical note for whoever designs this:* the Photos library is an opaque managed bundle — do **not** read its internals directly; use **PhotoKit** (`PHPhotoLibrary` authorization → `PHAsset` enumeration → `PHAssetResourceManager`/`requestImageDataAndOrientation` to pull the **original** file bytes + capture date/location). This is a new `ImportSource` implementation feeding the same import pipeline (copy → hash → dedup → place → manifest/catalog). Honors sovereignty: OpenPhoto copies *out* into plain files; it never writes back into the Photos library. Dedup-on-import (existing hash logic) prevents re-importing the same shots. Consider Live Photos (PhotoKit exposes both resources) and edited-vs-original (import the original, optionally the rendered edit).
- **[Phase 5 — CUT 2026-06-11]** **Preserve folder organization when sending photos back to a phone** (surfaced 2026-06-09, from usage feedback) — *cut in the 2026-06-11 Phase-5 review: infeasible over AirDrop without a companion iOS app (see below).* — when sending Mac/drive photos to an iPhone, OpenPhoto's **nested folder** structure is lost (they land in the camera roll). The user wants the organization to survive. *Hard constraint to research:* iOS Photos has **flat albums** (no nested folders, though "folders of albums" exist in the Photos app's own model), and **AirDrop cannot create or assign albums** — received photos go straight to Recents with no album. So true folder preservation over AirDrop is likely **not possible** as-is. Possible approaches to evaluate: (a) **flatten** a nested folder path into a single album name (e.g. `2022 › rome2022` → an album "2022/rome2022") — but album creation still can't be driven from the Mac side over AirDrop; (b) a small **companion iOS Shortcut/app** that watches for incoming OpenPhoto sends and files them into albums by an embedded hint; (c) embed a folder hint in metadata (e.g. a keyword/`dc:subject`) so a later on-device pass can sort. Record as **uncertain/research** — confirm the platform limits before committing; it may only be achievable with a companion app.
- **[3.5] Open-only `Vault` accessor for read-only drive queries** (surfaced 2026-06-10, Slice 4.5 code review) — every drive-read path on `AppState` (`openVault(for:)`, used by `fullResURL`, `evict`, `rehydrate`, `deleteDriveOnly`, and now `sendPlan`) calls `Vault.openOrCreate(at:role:.canonical)`, which **writes a fresh `vault.json` (with a new random vaultID)** when the present drive lacks one. For a properly adopted drive this never fires (the file exists → it just reads), and even in the anomalous wiped-`vault.json` case the new ID won't match `item.vaultID` so the asset safely falls through to unreachable/unavailable — but a *read-only* "what can I send / show?" query should never mutate a passive drive. Add a pure `Vault.open(at:)` (open-only; throws/returns nil if no `vault.json`) and switch the read-only callers (`openVault(for:)` and especially `sendPlan`/`fullResURL`, which run on sheet-open / viewer-load) to it, leaving `openOrCreate` only for genuine adoption. Small `OpenPhotoCore` + `AppState` change with its own tests; no format impact (it *reduces* incidental drive writes). Aligns with the "drives are passive" invariant.
- **[3.5] Choose among multiple connected devices when sending** (surfaced 2026-06-10, from usage feedback) — the Send action targets a **single** device: `AppState.connectedSendTarget()` returns the *first* connected device that can receive (cameras/AirDrop preferred), and the toolbar/selection Send button is wired to just that one. When several receivers are connected (e.g. two iPhones, or a phone + an SD card/volume), the user can't pick which to send to. Add a **device picker** at send time (a menu/sheet listing all connected send targets — phones via AirDrop, volumes via copy — by friendly name), defaulting to the current auto-pick. `SendSheet` already takes an explicit `device`, so the change is mostly in the entry points (`connectedSendTarget()` → "list connected targets" + a chooser) — no change to `SendEngine`/destinations. Browse/Send-UI addition; no data/format impact.
- **[3.5] `ISO8601Millis.dateLenient` rejects the app's own fractional-second timestamps** (surfaced 2026-06-10, Slice 5b code review) — `ISO8601Millis.string(from:)` writes timestamps **with** fractional seconds (`.withFractionalSeconds`), but `dateLenient(from:)` uses a formatter configured **without** them, so re-parsing the app's own `manifest.jsonl` `mtime` (or any value it wrote) returns `nil`. Consequence today: `CatalogSnapshot.verifyAdoption`'s minimal-asset `takenAtMs` always falls back to `0` (those stale-snapshot edge assets sort to the bottom of the timeline until a rescan regenerates real metadata) — benign and spec-accepted, but the "lenient" name is misleading and any other caller mis-parses too. One-line fix: make `dateLenient` try the fractional formatter first, then the non-fractional (`date(from:) ?? lenient.date(from:)`), so it accepts both — strictly more lenient, can't break existing parses. Core util change with its own test; no data/format impact.
- **[3.5 — DONE 2026-06-10] Multi-drive consensus repair in Verify Integrity** (surfaced 2026-06-10, from usage feedback during 5c design) — when **Verify Integrity** / **Check** finds drift or bit-rot on a drive, use the *other* connected drives (canonical + backups) and the Mac as repair sources: surface drift across all connected drives at once and offer **one-click repair from a verified-good copy**. *Not* a majority vote — under *canonical-is-truth*, the **canonical's** `manifest.jsonl` defines what's correct, and any connected copy whose bytes hash-match the expected hash is a valid repair source (re-verified on restore, so a rotten copy fails safe). The building block already exists (`goodCopyURL`/`DriftReconciler.restore` search the Mac + every connected durable drive for a hash-matching copy); this is a **Verify-Integrity UX enhancement** — run verification across the connected set, group findings, and drive a canonical-authoritative repair — layered on the Slice 2 drift/integrity engine, not on 5c's role management. No data/format impact.
- **[Phase 4 — DONE (Slice 4.5), 2026-06-11]** Re-verify "sent to device" Locations against the device on connect (see §10 Slice 4.5 + the 2026-06-11 changelog; plan `docs/superpowers/plans/2026-06-11-phase4.5-locations-reverify.md`) — the inspector **Locations** panel shows a "sent to *Jude's iPhone*" indicator from `sends.jsonl` (a *historical* confirmed send). Make it **honest going forward**: whenever a device (phone via ImageCaptureCore, SD card / volume, etc.) is plugged in, re-enumerate it and check whether each believed-present file is *still there* — then the indicator can say "**on this device** (confirmed *date*)" if present, or **drop/downgrade** to "sent *date* (no longer on the device)" if the user has since deleted it. Reuses the existing device enumeration (the AirDrop send-verification already re-enumerates by size+capture-date fingerprint; volumes can hash-match) + `sends.jsonl` + the `PresenceService`/Locations panel. Likely lands with **Phase 4**'s Locations/People work (presence is already a catalog table). The check is per-device-connect (cheap, fingerprint-based), updates the cached presence/Location state, and never writes to the device. No on-disk format impact (machine-derived presence is rebuildable).
- **[Phase 5? — largely superseded] Library-wide dedup review surface** (unbuilt from the original design, §7) — the design lists a "dedup review (identical-hash groups)" UI surface, but only *dedup-on-import* (Phase 2's hash check) was ever built; a standalone surface that groups existing identical-hash assets *across the whole library* was not. **Mostly covered now (2026-06-12 assessment):** the Phase-5 **Tidy Up → Duplicates** mode already groups duplicates (perceptual dHash, keeper = highest resolution) — but *within the same folder*. The only remaining gap is **byte-identical copies in *different* folders**, which in OpenPhoto are usually **intentional** (a photo can deliberately live in two folders; the catalog stores the bytes once by hash), so the value is low. **Likely drop / very low priority.** No format/data impact if ever built.
- **[Phase 5 — future] Embed OpenPhoto's own human metadata as standard in-file metadata (not just sidecars)** (surfaced 2026-06-12, during the new-import-sources brainstorm) — today OpenPhoto's rating/favorite/caption/tags/faces live in `.openphoto/*.xmp` **sidecars**, already as fully standard, portable XMP (`xmp:Rating`, `xmp:Label="Favorite"`, `dc:description`, `dc:subject`, `mwg-rs:Regions` — *not* Mac/Finder-specific; the Finder-tag xattr is the only Apple-specific bridge and is opt-in/separate). The user asked whether ongoing rating/favorite/caption could instead live **embedded inside the JPEG/HEIC** so the file metadata *is* the photo metadata. *Why deferred, and the hard tradeoff to resolve first:* the catalog, dedup, drive-sync, and manifests all key on each file's **content hash**, so embedding human edits would **rewrite the original and change its hash on every star/caption change** → every connected drive re-copies it, manifests churn, and it brushes invariant #1 (never modify originals) and #2 (human-authored → sidecars). The **import-time fold** (this slice) is the safe exception because it happens once, before the file is a tracked original. Standards reality to remember: **rating (`xmp:Rating`) and caption (`dc:description`) are genuinely native, universally-read embedded fields in JPEG/HEIC; "favorite" has *no* standard field** (only conventions: `xmp:Label="Favorite"` or aliasing to `xmp:Rating=5`). Any future design must pick a hash-stability strategy (e.g. exclude metadata blocks from the identity hash, or a separate "metadata-version" concept) before embedding ongoing edits.
- **[Phase 5.5 — DONE 2026-06-12] Move photos between folders (organize in the Folders screen)** (surfaced 2026-06-12; design refined 2026-06-12 by Jude) — **Shipped 2026-06-12** (spec: `2026-06-12-select-move-photos-design.md`) — OpenPhoto can move whole *folders* (`VaultReorganizer.moveFolder` + the folder-reorg drag), but there's **no way to move individual photos between folders**. This is the deliberate organization step after a **flat** library import (and broadly useful on its own). **Jude's design (the build target):** a Folders-screen-only **organize mode**. Dragging is overloaded — normally a drag = rubber-band *select* — so a **mode/button toggle** is needed: (1) drag to multi-select photos (reuse the existing modular selection — rubber-band / shift), then (2) move the selection by **either** of two mechanisms: **(a)** click a button that flips dragging into *move* mode, then **drag the selected photos onto a folder in the left folder-tree menu** (drag-and-drop move); **(b)** a separate **folder dropdown** destination picker + move button — pick (or create) a destination folder, *exactly like the import screen's destination selector*. Two independent ways to move. *Implementation sketch:* a new **per-photo atomic move** primitive — `moveFolder`'s machinery at file granularity: `FileManager.moveItem` the file **plus its `.openphoto/<name>.xmp` sidecar**, carry the **Live-pair partner** with it, collision-safe naming via the import engine's `collisionFreeURL`, then a manifest/catalog update (or a targeted rescan). The left folder-tree (`FolderTreeView`) already has drop-target plumbing from folder-reorg to reuse. Explicit user action moving real files → honors invariant #1; a same-volume move is an atomic rename. No on-disk format change (it relocates existing files + their sidecars).
- **[Phase 5.5 — DONE 2026-06-12] Import from *other people's* libraries — their Apple Photos/iCloud and their OpenPhoto drives (per-folder)** (surfaced 2026-06-12) — **Shipped 2026-06-12** (spec: `2026-06-12-foreign-imports-design.md`) — the new-import-sources slice covers **your own** signed-in Mac (Apple Photos/iCloud via PhotoKit) and **your own** Google Takeout. Two adjacent wants for later: (a) **another person's Apple Photos / iCloud** — e.g. import shares/handoffs from a friend's library. PhotoKit only sees the Mac's signed-in account, so this needs a different path (a shared iCloud album, an exported `.photoslibrary`/Takeout-style dump they hand you, or AirDrop) — research the sovereignty-correct ingress. (b) **another person's OpenPhoto canonical/backup drive** — since an OpenPhoto drive is just standard files + `manifest.jsonl`/`vault.json`, you should be able to plug in someone else's drive and **import selected folders** from it (a **per-folder import**, not a whole-library adopt), copying chosen subtrees into your own library through the same copy→hash→dedup→place pipeline, read-only on their drive (drives are passive). Likely a new `ImportSource`/browse-and-pick surface over a foreign vault. No format change anticipated (it *reads* the documented vault format).

## 11. Known risks & spikes

- **iPhone deletion over USB**: ~~iOS restricts ImageCaptureCore deletion when iCloud Photos is enabled~~ **RESOLVED by spike (2026-06-08, see `docs/spikes/2026-06-08-icc-deletion.md`)**: deletion via `requestDeleteFiles` SUCCEEDED with iCloud Photos ON on Jude's device. Phase 2 may offer "Delete from iPhone" after verified import, with per-item failure surfacing as a fallback for other configurations. The locked-phone state (error −9943) must be handled by waiting for `cameraDeviceDidRemoveAccessRestriction` and retrying; device item order is not chronological — sort by `creationDate`.
- **MobileCLIP model selection/conversion**: verify a Core ML build with acceptable quality and a usable text encoder before phase 4.
- **Offline reverse-geocode dataset**: choose one (~tens of MB, city-level is sufficient) before phase 4.
- **HEIC/Live Photo edge cases**: pairing relies on Apple's content identifier; some third-party apps strip it. Fallback heuristic: same basename + adjacent timestamps.
