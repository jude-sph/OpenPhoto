# Brainstorming companion sessions
.superpowers/

# macOS
.DS_Store

# Xcode / Swift
.build/
build/
DerivedData/
*.xcuserstate
xcuserdata/

# Synthetic validation library and fixture card (generated, never real photos)
fixtures-library/
fixtures-card/
spike-download/

# On-device ML models + datasets (downloaded via tools/ fetch scripts, never committed;
# make-app.sh injects them into the built .app, the build tolerates their absence)
.models/
Sources/OpenPhotoCore/Resources/*.mlpackage/
