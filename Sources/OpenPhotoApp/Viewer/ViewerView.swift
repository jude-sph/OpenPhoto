import SwiftUI
import OpenPhotoCore

struct ViewerView: View {
    @Bindable var state: AppState
    var body: some View { Text("Viewer — replaced in Task 20").frame(maxWidth: .infinity, maxHeight: .infinity) }
}
