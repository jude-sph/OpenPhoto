import SwiftUI
import OpenPhotoCore

struct TimelineView: View {
    @Bindable var state: AppState
    var body: some View { Text("Timeline — replaced in Task 18").frame(maxWidth: .infinity, maxHeight: .infinity) }
}
