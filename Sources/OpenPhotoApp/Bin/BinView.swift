import SwiftUI
import OpenPhotoCore

struct BinView: View {
    @Bindable var state: AppState
    var body: some View { Text("Bin — replaced in Task 22").frame(maxWidth: .infinity, maxHeight: .infinity) }
}
